Note: Change the directories of datasets in the python files for proper execution.

1. First run the "Emotion Detection.py".

2. Then, run the "AgeGenderModel.py".

3. Two files with "h5" extension will be created.

4. Now, run the "DriverFile.py" to predict emotion, age and gender.

5. To exit the project, press "q".
